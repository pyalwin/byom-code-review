{
  "name": "byom-code-review",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Bring Your Own Model — code review with any AI model via OpenRouter.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
